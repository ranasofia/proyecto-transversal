export class Conductor {
    /**
     * Variable de tipo number que representa el id del conductor
     */
    idConductor: number;

    /**
     * Variable de tipo string que representa el nombre del conductor
     */
    nombre: string;
    
    /**
     * Variable de tipo string que representa el apellido del conductor
     */
    apellido: string;
    
    /**
     * Variable de tipo string que representa la fecha de nacimiento del conductor
     */
    fechaDeNacimiento: string;

    /**
     * Variable de tipo string que representa el correo del conductor
     */
    email: string;

    /**
     * Variable de tipo string que representa la placa del conductor
     */
    placa: string;
    
    /**
     * Variable de tipo string que representa el celular del conductor
     */
    celular: string;
    
    /**
     * Variable de tipo string que representa el nombre de usuario del conductor
     */
    usuario: string;
    
    /**
     * Variable de tipo string que representa la contreseña de la cuenta del conductor
     */
    contrasena: string;
    
    /**
     * Variable de tipo string que representa el estado en el cual se encuentra el conductor
     */
    sesion: string;
    
    /**
     * Variable de tipo number que representa el estado de disponibilidad en el cual se encuentra el conductor
     */
    idEstado: number;
    
    /**
     * Variable de tipo string que representa la cedula del conductor
     */
    cedula: string;

    /**
     * Variable de tipo string que representa la fecha de sansion del conductor
     */
    fechaSancion: string;
    
    /**
     * Variable de tipo number que representa el rol del conductor
     */
    rol: number;
}